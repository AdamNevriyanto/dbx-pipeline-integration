resources:
  pipelines:
    dummy_sdp_pipeline:
      name: dummy_sdp_pipeline
      catalog: mii_workspace
      root_path: ${workspace.file_path}/pipelines/dummy_sdp_pipeline/pipeline_btn_development
      environment:
        dependencies:
          - -e ${workspace.file_path}/utils
      libraries:
        - glob:
            include: ${workspace.file_path}/pipelines/dummy_sdp_pipeline/pipeline_btn_development/**
